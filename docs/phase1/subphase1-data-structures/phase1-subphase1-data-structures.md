# Phase 1, Subphase 1: Data Structures

This document describes the data structures that underpin the syntactic parsing phase. These types are defined before the lexer or parser are built, since both depend on them.

Scope:
- Source location and span types
- Token kinds and the Token type
- AST node types
